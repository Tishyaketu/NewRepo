import React from "react";

const Navbar: React.FC = () => {
  return (
    <nav>
      <h2>Task Management</h2>
    </nav>
  );
};

export default Navbar; // ✅ Add export to make it a module
